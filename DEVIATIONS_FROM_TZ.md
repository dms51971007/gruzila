## Отклонения от ТЗ для `gruzilla-executor`

Этот файл фиксирует все известные отличия текущей реализации от исходного ТЗ по компоненту `gruzilla-executor` и сопутствующим частям.

### 1. Поддерживаемые типы шагов сценария

- **Соответствие ТЗ**
  - Поддерживаются шаги типов `rest` и `kafka` (как и описано в ТЗ).
- **Расширение ТЗ**
  - Добавлен тип шага `db` для проверки записей в PostgreSQL (поля `db_dsn`, `db_query`, `assert.rows`).
- **Расширение ТЗ**
  - Реализован тип шага `mq` для взаимодействия с ActiveMQ Artemis через STOMP:
    - `mq_action: put|get`;
    - параметры подключения `mq_conn_name`, `mq_user`, `mq_password`;
    - ожидание ответа `mq_wait_ms`;
    - поддержка assert-проверок в `mq get` (в т.ч. `success`, `requestId`, `clientGuid`);
    - поддержка отправки пользовательских заголовков в `mq put` через `mq_headers` (динамический набор ключей, интерполяция `{{var}}` и `{var}`);
    - поддержка broker-side селектора в `mq get` через `mq_selector`:
      - можно передавать готовое выражение селектора;
      - можно передавать имя поля (например `RequestId`), тогда формируется выражение `RequestId = '<value>'`.
- **Отклонение от ТЗ**
  - Валидация YAML остаётся «мягкой»: для `type: mq` на этапе `scenario.Validate` не проверяются обязательные MQ-поля; ошибки выявляются в рантайме исполнителя шага.

### 2. Метрики и их API

- **Что было в ТЗ**
  - Требовались: время работы, количество успешных/неуспешных итераций, текущий TPS и «прочие агрегаты».
  - Отдача через `POST /api/v1/metrics` в формате `{ "status", "data" }`.
- **Как сделано сейчас**
  - Структура `Metrics`:
    - `attempts_count` — всего запущено попыток сценария (success + error + in-flight).
    - `success_count` — количество успешно завершённых сценариев.
    - `error_count` — количество завершившихся ошибкой сценариев (включая паники в шагах).
    - `last_latency_ms` — латентность последнего завершившегося запуска сценария.
    - `adaptive_tps` — динамический ceiling (ограничитель), до которого может быть урезан целевой TPS.
    - `target_tps` — целевой TPS в текущую секунду (результат функции `effectiveTPS` после учёта процента и ramp-up).
    - `current_tps` — **реальный TPS**: прирост `attempts_count` за последнюю секунду (дельта между тиками), а не «запланированное» число итераций.
  - Метрики отдаются:
    - через `POST /api/v1/metrics` — только `Metrics`;
    - дублируются внутри `Status` в ответах `start/stop/update/status/reload`.
- **Отклонения**
  - В явном виде **нет поля «время работы»**, вместо него:
    - в `Status` есть `started_at` (время старта нагрузки), время работы получается как `now - started_at` на стороне клиента.
  - Появились дополнительные поля (`attempts_count`, `target_tps`), которых не было в ТЗ — это **расширение** для диагностики нагрузки.

### 3. Модель нагрузки и ramp-up

- **ТЗ**
  - В сценарии задаётся **базовый TPS** и **скорость набора TPS** (пример из ТЗ: `10 TPS/min`).
  - CLI передаёт коэффициент (1–500%) и, при необходимости, уточнённый TPS.
- **Реализация**
  - В `RunConfig`:
    - `percent` — коэффициент нагрузки (если ≤ 0, подставляется 100);
    - `base_tps` — базовый TPS сценария (если ≤ 0, подставляется 1);
    - `ramp_up_seconds` — длительность **линейного** набора нагрузки от 0 до `base_tps * percent/100`.
  - Эффективный TPS считается функцией:
    - `effectiveTPS(base_tps, percent, ramp_up_seconds, started_at)`.
- **Отклонения**
  - Вместо «скорости набора TPS в TPS/минуту» используется **время разгона в секундах** (`ramp_up_seconds`), с **линейной** функцией роста.
  - На серверной стороне **нет жёсткой валидации** диапазона `percent` (ТЗ говорило 1–500%). Значения за пределами этого диапазона технически возможны, объектно не блокируются.

### 4. HTTP API executor'а

- **Соответствие ТЗ**
  - Все основные эндпоинты реализованы:
    - `POST /api/v1/start`
    - `POST /api/v1/stop`
    - `POST /api/v1/update`
    - `POST /api/v1/status`
    - `POST /api/v1/metrics`
  - Формат ответа соответствует стандарту REST API проекта:
    - `{ "status": "success" | "error", "data"?: ..., "error"?: "..." }`.
  - Префикс `/api/v1/` и метод `POST` соблюдены.
- **Расширения ТЗ**
  - Добавлен эндпоинт `POST /api/v1/reload`:
    - перечитывает YAML-сценарий с диска без остановки нагрузки;
    - обновляет имя сценария и очищает `last_error`.
  - Добавлен эндпоинт `POST /api/v1/reset_metrics`:
    - обнуляет счётчики (`attempts_count`, `success_count`, `error_count`), `last_latency_ms`, `last_error`, `started_at`; разрешён только при остановленной нагрузке (`running: false`).
  - Добавлен эндпоинт `POST /api/v1/shutdown`:
    - инициирует корректное завершение процесса executor (вызов `http.Server.Shutdown`), чтобы CLI мог перезапустить процесс с новым кодом.
  - Добавлен эндпоинт **GET /metrics** (не POST, без авторизации):
    - отдача метрик в формате Prometheus для сбора сборщиком (Prometheus) и визуализации в Grafana.
  - В `Status` дополнительно отдаётся:
    - `scenario_path` — путь до файла сценария;
    - `started_at` — момент старта нагрузки.

### 5. Поведение при ошибках и паниках

- **ТЗ (ожидаемое поведение)**
  - Явного описания паник нет, подразумевалось, что ошибки шагов считаются в `error_count`.
- **Реализация**
  - Внутри выполнения сценария добавлен `recover()`:
    - паника в любом шаге оборачивается в ошибку вида `panic: ...`;
    - такое выполнение учитывается в `error_count` и попадает в `last_error`.
- **Отклонения**
  - Это поведение является **уточнением/расширением** ТЗ: паники явно трактуются как ошибки сценария и учитываются в метриках, чтобы не «терять» попытки при высокой нагрузке.

### 6. Счётчики под высокой нагрузкой

- **ТЗ**
  - Требовались корректные счётчики успешных/неуспешных итераций и текущий TPS.
- **Реализация**
  - `success_count`, `error_count`, `attempts_count`, `last_latency_ms` реализованы на **атомарных** счётчиках (`sync/atomic`), чтобы:
    - избежать потери инкрементов при большом числе параллельных горутин;
    - корректно подсчитывать общее количество попыток.
- **Отклонения**
  - Это техническое отличие реализации (ТЗ не регламентировало механизм), по функционалу соответствует/расширяет требования.

### 6.1. Планировщик итераций под деградацией внешних систем

- **ТЗ**
  - Явно не регламентировало внутренний механизм планирования горутин на тик.
- **Реализация**
  - Убран блокирующий `wg.Wait()` на каждом тике `runLoop`.
  - Добавлен лимитер `in-flight`-итераций (буферизированный канал), чтобы:
    - не блокировать основной цикл планирования;
    - не создавать неограниченное число горутин при деградации внешних зависимостей.
- **Отклонения**
  - Это архитектурное расширение относительно базовой модели «batch-per-tick»: повышает живучесть под перегрузкой, но может приводить к пропуску части «плановых слотов» тика при насыщении лимитера.

### 6.2. Адаптивное ограничение TPS

- **ТЗ**
  - Требовало базовый TPS и ramp-up, но не описывало self-throttling.
- **Реализация**
  - Добавлен адаптивный потолок `adaptive_tps`, который ограничивает `target_tps` при просадках.
  - Алгоритм использует плавную деградацию/восстановление, чтобы не «залипать» на минимальном TPS после кратковременных сбоев.
- **Отклонения**
  - Расширение ТЗ: фактический целевой TPS может быть ниже заданного `base_tps * percent / 100`, если система не успевает обрабатывать нагрузку.

### 6.3. MQ под высокой нагрузкой (Artemis/STOMP)

- **ТЗ**
  - Не задавало конкретную стратегию соединений для MQ-транспорта.
- **Реализация**
  - Переиспользование соединений и подписок для `mq get`.
  - Добавлен минимальный пул STOMP-соединений для `mq put` (round-robin), чтобы снизить `msg send timeout` на высоком TPS.
  - На transport-ошибках предусмотрен сброс кэшей/пересоздание соединений.
  - Для `mq_selector` используется broker-side фильтрация на уровне `SUBSCRIBE` (header `selector`), чтобы «чужие» сообщения не попадали в consumer.
  - Для динамических selector-подписок используется ephemeral-подход (без кэша + `Unsubscribe`), чтобы не накапливать consumers на брокере.
- **Отклонения**
  - Расширение ТЗ в сторону эксплуатационной устойчивости и изоляции трафика сценариев на больших TPS.

### 7. Шаблоны и переменные

- **ТЗ**
  - Описывалось использование шаблонов из папки `templates/` и подстановки переменных `{{var}}` в шагах.
- **Реализация**
  - Шаблоны для тел сообщений реализованы через отдельный пакет `internal/templates` и поле `template` в шаге:
    - если `template` задан, тело шага рендерится через Go `text/template` с передачей всех переменных;
    - если нет — используется поле `body` с простой подстановкой `{{var}}`.
- **Отклонения**
  - Существенных функциональных отклонений от ТЗ нет; реализация местами богаче (кэширование шаблонов, пул буферов), но это детали реализации.

### 8. Prometheus и мониторинг

- **ТЗ**
  - Не описывало интеграцию со сборщиком метрик или Grafana.
- **Реализация**
  - В executor добавлена отдача метрик Prometheus: **GET /metrics** (формат Prometheus text).
  - Метрики (namespace `gruzilla_`, лейбл `scenario`): `attempts_total`, `success_total`, `errors_total`, `current_tps`, `target_tps`, `last_latency_ms`, `running`.
  - Обновление: каждый тик в runLoop и при Stop(); значения синхронизированы с атомарными счётчиками и статусом.
  - В репозитории добавлены `docker-compose.monitoring.yml` и `monitoring/prometheus.yml` для запуска Prometheus и Grafana; в Grafana подключается datasource Prometheus (`http://prometheus:9090`), для «в секунду» используются запросы вида `rate(gruzilla_success_total[1m])`.
- **Отклонения**
  - Расширение ТЗ: полноценный стек мониторинга (Prometheus + Grafana) не был обязателен по ТЗ.

### 9. CLI: перезапуск executor и сброс метрик

- **ТЗ**
  - CLI управляет нагрузкой через API executor'а (start, stop, update, status); отдельно не регламентировались «перезапуск процесса» и «сброс счётчиков».
- **Реализация**
  - **`run reset-metrics`** — вызов `POST /api/v1/reset_metrics` для обнуления счётчиков при остановленной нагрузке.
  - **`executors restart`** — отправка `POST /api/v1/shutdown` текущему процессу, ожидание ~2 с, запуск нового процесса executor с теми же `--scenario` и `--addr` (чтобы подхватить обновлённые исходники).
  - Добавлен флаг `--log-file` для `gruzilla-executor`, `gruzilla-cli executors start/restart` и проброс через backend API:
    - позволяет писать runtime-логи executor в файл (включая MQ-логи сообщений/headers);
    - в backend добавлены дефолтные настройки `executor_logs_enabled` и `executor_log_file` (с шаблоном `{addr}`).
- **Отклонения**
  - Расширение ТЗ: удобство эксплуатации (перезапуск без ручного поиска PID, сброс метрик без повторного start/stop).

### 10. Документация сценария Artemis request-reply

- **ТЗ**
  - Отдельный README для конкретного сценария явно не требовался.
- **Реализация**
  - Добавлен файл `scenarios/mq-topic1-request-reply.README.md` с практическими командами:
    - запуск `gruzilla-executor` для сценария;
    - все команды `gruzilla-cli run` и `gruzilla-cli executors` с примерами;
    - типовые последовательности (start/update/status/stop/reload/reset).
- **Отклонения**
  - Расширение ТЗ: операционная документация для упрощения запуска и диагностики.

### 11. CLI CRUD для YAML-сценариев

- **ТЗ**
  - Описывало управление нагрузкой через API executor, но не требовало файлового CRUD сценариев через CLI.
- **Реализация**
  - В `gruzilla-cli` добавлена группа `scenarios` с командами:
    - `scenarios list` — список `.yml/.yaml` в директории;
    - `scenarios read` — чтение файла сценария;
    - `scenarios create` — создание файла (из шаблона, `--content` или `--from-file`);
    - `scenarios update` — обновление файла (`--content` или `--from-file`);
    - `scenarios delete` — удаление файла с подтверждением `--yes`.
  - Для `create/update` добавлена проверка корректности YAML и базовой валидности структуры `Scenario`.
- **Отклонения**
  - Расширение ТЗ: управление жизненным циклом YAML-файлов вынесено в CLI для снижения ручных операций с файлами.

### 12. CLI CRUD для шаблонов

- **ТЗ**
  - Не требовало отдельного файлового CRUD для шаблонов через CLI.
- **Реализация**
  - В `gruzilla-cli` добавлена группа `templates` с командами:
    - `templates list` — список файлов шаблонов в директории;
    - `templates read` — чтение файла шаблона;
    - `templates create` — создание шаблона (`--content`, `--from-file` или дефолтный template);
    - `templates update` — обновление файла (`--content` или `--from-file`);
    - `templates delete` — удаление файла с подтверждением `--yes`.
- **Отклонения**
  - Расширение ТЗ: операции жизненного цикла шаблонов вынесены в CLI для удобства эксплуатации.

### 13. Суточное расписание нагрузки, игнор расписания, плейсхолдеры и лог трафика

- **ТЗ**
  - Не описывало суточный профиль нагрузки по часам, отдельный флаг «без расписания», шаблоны даты вида `{{__now:...}}` и отдельный файл лога трафика шагов.
- **Реализация**
  - В модели сценария: `load_schedule` (inline) или `load_schedule_profile` (YAML-include с `max_load`, `timezone`, `intervals`; ключи интервалов — часы 0–23).
  - В `RunConfig` / API: `ignore_load_schedule` — при `true` расписание не применяется, целевой TPS считается только из `base_tps` и `percent`.
  - В `Status`: `scenario_has_load_schedule` — признак наличия активного расписания в загруженном сценарии.
  - CLI: `run start --ignore-load-schedule`, `run update --ignore-load-schedule` / `--ignore-load-schedule=false`.
  - Плейсхолдеры в строках шагов: `{{__now:LAYOUT}}`, `{{__randDigits:N}}`, `{{__randHex:N}}`.
  - При старте executor с `--log-file` — дополнительное логирование трафика шагов в файл.
- **Отклонения**
  - Расширение ТЗ: операторский контроль нагрузки по времени суток и обход расписания без правки YAML.

---

Если в ТЗ появятся обновления или обнаружатся ещё расхождения (например, по формату дополнительных полей в ответах), этот файл нужно дополнять, чтобы оставался единый источник правды по отличиям.

